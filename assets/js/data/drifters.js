/* ============================================================
   DRIFTERS - generated by `npm run series add`.
   Season-level catalogue; the per-episode data lives in
   assets/js/data/series/drifters.js.
   ============================================================ */

(() => {

const ITEMS = [
  { w: 1, id: 's68103-1', title: 'Drifters - Season 1', release: '2016-10-07',
    mins: 244, eps: 15, type: 'season', phase: 1, chrono: 1, cLabel: 'Season 1', rel: 'essential' },
];

const TYPE_META = {
  'season': { label: 'Season', short: 'SEASON', color: '#6a9ee8' },
};

const REL_META = {
  essential: { label: 'Essential', rank: 0, color: '#6a9ee8', blurb: 'Part of the main run.' },
};

const SAGA_META = {};

const PHASE_META = {
  1: { label: 'Drifters', sub: '2016' },
};

const WATCH_BLOCKS = [
  { max: 1, phase: 1 },
];

const ERAS = [
  { max: 9999, key: 'all', title: 'Drifters', sub: 'Every season' },
];

const POSTERS = {};

(window.CATALOGUES ||= {}).drifters = {
  items: ITEMS,
  posters: POSTERS,
  imgDir: 'assets/img/drifters/',
  types: TYPE_META,
  rel: REL_META,
  saga: SAGA_META,
  phase: PHASE_META,
  watchBlocks: WATCH_BLOCKS,
  eras: ERAS,
};

})();
