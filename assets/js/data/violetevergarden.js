/* ============================================================
   VIOLET EVERGARDEN - generated by `npm run series add`.
   Season-level catalogue; the per-episode data lives in
   assets/js/data/series/violetevergarden.js.
   ============================================================ */

(() => {

const ITEMS = [
  { w: 1, id: 's75214-1', title: 'Violet Evergarden - Season 1', release: '2018-01-11',
    mins: 319, eps: 13, type: 'season', phase: 1, chrono: 1, cLabel: 'Season 1', rel: 'essential' },
];

const TYPE_META = {
  'season': { label: 'Season', short: 'SEASON', color: '#6a9ee8' },
  'film':   { label: 'Film',   short: 'FILM',   color: '#c2453f' },
};

const REL_META = {
  essential:   { label: 'Essential',   rank: 0, color: '#6a9ee8', blurb: 'Part of the main run.' },
  recommended: { label: 'Recommended', rank: 1, color: '#e8913d', blurb: 'Sits alongside the main run.' },
};

const SAGA_META = {};

const PHASE_META = {
  1: { label: 'Violet Evergarden', sub: '2018' },
};

const WATCH_BLOCKS = [
  { max: 1, phase: 1 },
];

const ERAS = [
  { max: 9999, key: 'all', title: 'Violet Evergarden', sub: 'Every season' },
];

const POSTERS = {};

(window.CATALOGUES ||= {})['violetevergarden'] = {
  items: ITEMS,
  posters: POSTERS,
  imgDir: 'assets/img/violetevergarden/',
  types: TYPE_META,
  rel: REL_META,
  saga: SAGA_META,
  phase: PHASE_META,
  watchBlocks: WATCH_BLOCKS,
  eras: ERAS,
};

})();
