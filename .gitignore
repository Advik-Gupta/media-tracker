node_modules/
.DS_Store

# Vercel's local link/config, created by `vercel` or `vercel dev`
.vercel
.env
.env.local

# Written by the build from the environment
assets/js/config.js

# Local scratch from the enrichment and poster scripts. The results are
# committed inside assets/js/data; these are just the working files.
build/.rg-cache.json
build/*.tsv
sweep_out.tsv
*.log
CLAUDE.MD
AGENTS.MD